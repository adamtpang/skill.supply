# Claude API key — https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# WhatsApp number for the site-wide beta bar (digits only, country code first).
# Unset or empty: the beta bar falls back to email.
NEXT_PUBLIC_ADAM_WA=
